Marc Maurice
Projet de manipulation du bash, markdown et des commandes git
Liste des commandes git apprises cette semaine : git commit -m , git log, git add. , git branch, git checkout

pour changer de branches, git remote pour synchroniser avec github, git push -u pour pusher, git remote -v pour verifier la 
connexion et git pull pour ramener les fichiers en ligne sur l ordi local

